# ============================================================
# Environment Configuration
# ============================================================
# development - Local API server with MSW mocking
# staging     - Staging AWS backend
# production  - Production AWS backend
NODE_ENV=development

# ============================================================
# API Configuration
# ============================================================
# Development: Local API server or MSW mocking
# Staging/Production: Update with CDK outputs after deployment
VITE_API_URL=http://localhost:3001/api
VITE_WS_URL=ws://localhost:3001/ws

# ============================================================
# Authentication Mode
# ============================================================
# 'saas' - Use Amazon Cognito (requires Cognito config below)
# 'self-hosted' - Use local JWT authentication
VITE_AUTH_MODE=self-hosted

# ============================================================
# Cognito Configuration (required if VITE_AUTH_MODE=saas)
# ============================================================
# Get these values from CDK outputs after deployment:
#   cd infra && NODE_ENV=staging npx cdk outputs --all
#
# VITE_COGNITO_USER_POOL_ID=us-east-1_xxxxxxxxx
# VITE_COGNITO_CLIENT_ID=xxxxxxxxxxxxxxxxxxxxxxxxxx
# VITE_COGNITO_REGION=us-east-1
# VITE_COGNITO_DOMAIN=accessagents-123456789012

# ============================================================
# Development Options
# ============================================================
# Enable Mock Service Worker for frontend development without backend
# Set to 'true' for local development, 'false' for production
VITE_ENABLE_MSW=true
